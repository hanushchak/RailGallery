@model IEnumerable<RailGallery.Models.Locomotive>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.LocomotiveModel)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LocomotiveBuilt)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LocomotiveModel)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LocomotiveBuilt)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.LocomotiveID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.LocomotiveID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.LocomotiveID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
